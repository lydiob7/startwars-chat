const routes = {
    home: "/",
    auth: "/auth",
    authCallback: "/auth/callback",
    login: "/auth/login",
    profile: "/profile",
    signup: "/auth/signup"
} as const;

export default routes;
